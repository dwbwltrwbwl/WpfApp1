<Page x:Class="WpfApp1.Pages.DataOutput"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:WpfApp1.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Рецепты">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height ="Auto"/>
            <RowDefinition Height ="5*"/>
            <RowDefinition Height ="1*"/>
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal" VerticalAlignment="Top" Margin="10">
            <TextBox x:Name="TextSearch" TextWrapping="Wrap" Width="186" Height="20" ToolTip="Введите текст для поиска рецептов"/>
            <Button Content="Применить" Width="120" Height="20" Margin="5,0,0,0" Click="ApplySearch_Click"/>
            <Button Content="Сбросить" Width="120" Height="20" Margin="5,0,0,0" Click="ResetSearch_Click"/>

            <ComboBox x:Name="ComboSort" Height="20" Width="311" Margin="5,0,0,0" SelectionChanged="ComboSort_SelectionChanged">
                <ComboBoxItem Content="Сортировать по имени"/>
                <ComboBoxItem Content="Сортировать по времени приготовления"/>
            </ComboBox>
        </StackPanel>
        <ListView x:Name="listProducts" Grid.Row="1" ToolTip="Список доступных рецептов">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <WrapPanel>
                        <StackPanel Orientation="Vertical" HorizontalAlignment="Left" Margin="5">
                            <TextBlock Width="auto" TextWrapping="Wrap" Height="auto" Text="{Binding RecipeName}" FontWeight="Bold" FontSize="16" Foreground="#FF0027FF"/>
                            <TextBlock Width="auto" TextWrapping="Wrap" Height="auto">
                                <Run Text="Описание: "/>
                                <Run Text="{Binding DescriptionN}"/>
                            </TextBlock>
                            <TextBlock Width="auto" TextWrapping="Wrap" Height="auto">
                                <Run Text="Автор: "/>
                                <Run Text="{Binding Authors.AuthorName}"/>
                            </TextBlock>
                            <TextBlock Width="auto" TextWrapping="Wrap" Height="auto">
                                <Run Text="Категория: "/>
                                <Run Text="{Binding Categories.CategoryName}"/>
                            </TextBlock>
                            <TextBlock Width="auto" TextWrapping="Wrap" Height="auto">
                                <Run Text="Время приготовления: "/>
                                <Run Text="{Binding CookingTime}"/>
                            </TextBlock>
                            <Button Content="Редактировать" Width="120" Height="20" Click="EditButton_Click" ToolTip="Редактировать рецепт"/>
                        </StackPanel>
                    </WrapPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>
